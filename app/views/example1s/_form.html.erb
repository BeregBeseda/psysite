<%= form_for(@example1) do |f| %>
  <% if @example1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@example1.errors.count, "error") %> prohibited this example1 from being saved:</h2>

      <ul>
      <% @example1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
